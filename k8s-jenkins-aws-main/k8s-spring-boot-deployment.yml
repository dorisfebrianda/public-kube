apiVersion: apps/v1
kind: Deployment
metadata:
  name: doris-springboot
spec:
  replicas: 3
  selector:
    matchLabels:
      app: doris-springboot
  template:
    metadata:
      labels:
        app: doris-springboot
    spec:
      containers:
        - name: springboot
          image: dorisimagesdemotag:latest
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: doris-springboot
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: doris-springboot
